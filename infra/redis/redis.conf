# Redis 配置文件
# 生产环境配置

# 绑定地址
bind 0.0.0.0

# 端口
port 6379

# 持久化
appendonly yes
appendfsync everysec

# 内存管理
maxmemory 256mb
maxmemory-policy allkeys-lru

# 日志级别
loglevel notice

# 数据库数量
databases 16

# 超时设置（0表示不超时）
timeout 0

# TCP keepalive
tcp-keepalive 300

# 保护模式（在容器中关闭）
protected-mode no
